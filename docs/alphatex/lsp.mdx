--- 
title: 语言服务器
since: 1.7.0
---

alphaTab 通过 NPM 包 [`@coderline/alphatab-language-server`](https://www.npmjs.com/package/@coderline/alphatab-language-server) 提供了一个 alphaTex 语言服务器。
该语言服务器使用 TypeScript 编写，并作为 JavaScript 可执行文件提供。

> [!INFO]
> 要了解更多关于语言服务器和语言服务器协议的信息，请访问 Microsoft 的网站：
> https://microsoft.github.io/language-server-protocol/

该语言服务器使用 [`vscode-languageserver-node`](https://github.com/microsoft/vscode-languageserver-node) 实现，提供了一个用于创建在 Node.js 和浏览器中运行的语言服务器的基础框架。

下面你可以找到语言服务器和包提供的各个功能的描述。

## 诊断

语言服务器的核心功能是监听[文档更改](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#textDocument_synchronization)并解析文档的代码。

语言服务器提供[拉取诊断](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#textDocument_pullDiagnostics)，但不会[发布诊断](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#textDocument_publishDiagnostics)。

<img src="/img/alphatex/lsp/diagnostics.png" />

## 代码补全

语言服务器将提供上下文的[代码补全](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#textDocument_completion)。
这包括但不限于：

* 可用的元数据标签
* 可用的属性
* 可用的值（例如枚举）

<img src="/img/alphatex/lsp/completions1.png" />
<img src="/img/alphatex/lsp/completions2.png" />
<img src="/img/alphatex/lsp/completions3.png" />

## 文档悬停提示

对于每个有文档记录的元素，语言服务器还将提供文档悬停提示。类似于你在编程语言中编写文档注释时，
编辑器会在悬停在元素上时显示描述。无需退出编辑器去查找 alphaTex 的帮助，它就在你需要的地方。

<img src="/img/alphatex/lsp/hover1.png" />
<img src="/img/alphatex/lsp/hover2.png" />

## 签名帮助

在向元数据标签和属性传递参数时，语言服务器将提供签名帮助，以提供关于存在哪些参数和值的额外指导：

<img src="/img/alphatex/lsp/signature1.png" />
<img src="/img/alphatex/lsp/signature2.png" />

## alphaTex 语言定义

### TextMate 语法

为了支持在 VS Code 和 Monaco（以及其他兼容编辑器）中进行语法高亮，该包还提供了一个用于 alphaTex 语法的基本 TextMate 语法。
该语法作为 JavaScript 对象提供，可以转储到 JSON 中或直接传递给兼容的组件。

```ts
import { textMateGrammar } from '@coderline/alphatab-language-server';
import fs from 'node:fs';

await fs.promises.writeFile('alphatex.tmLanguage.json', JSON.stringify(textMateGrammar, null, 2));
```

### 语言配置

与 TextMate 语法类似，该包还提供了一个[语言配置](https://code.visualstudio.com/api/language-extensions/language-configuration-guide)，提供有关注释、括号等的详细信息。

```ts
import { languageConfiguration } from '@coderline/alphatab-language-server';
import fs from 'node:fs';

await fs.promises.writeFile('language-configuration.json', JSON.stringify(languageConfiguration, null, 2));
```

### 文档

该包还提供对所有 alphaTex 语言元素的原始访问。这些定义在内部用于提供文档悬停提示，但我们也使用
它们来生成你可以在侧边菜单中找到的语言参考页面。


```ts
import { documentation } from '@coderline/alphatab-language-server'

const tupletProperty = documentation.beatProperties.get('tu');
```