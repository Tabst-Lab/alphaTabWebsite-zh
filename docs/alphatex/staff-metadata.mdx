---
title: 谱表元数据
---

import { AlphaTexSample } from '@site/src/components/AlphaTexSample';
import { SinceBadge } from '@site/src/components/SinceBadge';

本页描述了所有影响数据模型“谱表”级别的元数据标签。

为避免任何意外的副作用，这些值应在第一个小节上指定。

其中一些标签只是历史上的元数据标签。它们将来可能会变成谱表属性。

import Doc from './_staff-metadata.mdx';

<Doc />