---
title: 乐谱
---

import { AlphaTab } from '@site/src/components/AlphaTab';

渲染标准乐谱是 alphaTab 的核心功能之一。

## 谱号
谱号对于乐谱来说也很重要。
<AlphaTab tex="true" settings={ {
	staveProfile: 'score',
	display: {
		hideInfo: true
	}
} }>
{`
\\title "谱号"
.
\\clef G2 | \\clef F4 | \\clef C3 | \\clef C4
`}
</AlphaTab>

## 调号
需要调号？没问题，alphaTab 会为您渲染。
<AlphaTab file="/files/features/KeySignatures.gp5" settings={ {
	staveProfile : 'score'
} } />

## 拍号
拍号也很重要。
<AlphaTab file="/files/features/TimeSignatures.gp5" settings={ {
	staveProfile : 'score'
} } />

## 音符、休止符和符杠
我们会自动为音符创建符杠，并确保它们以最易读的方式对齐。
<AlphaTab file="/files/features/Beams.gp" settings={ {
	staveProfile : 'score'
} } />

## 变音记号
变音记号是乐谱的基本要素。
<AlphaTab file="/files/features/Accidentals.gp5" settings={ {
	staveProfile : 'score'
} } />