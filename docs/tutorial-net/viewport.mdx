---
title: 主视口
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## 基本设置

随着我们的应用程序在本教程中不断增加新的 UI 元素，我们已经根据最终解决方案准备了大致的结构。

```xml
<Window
    x:Class="AlphaTabTutorial.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AlphaTabTutorial"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance local:MainWindow}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Canvas>
            <Border
                Canvas.Left="0"
                Canvas.Top="0"
                Width="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualWidth}"
                Height="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualHeight}"
                Padding="70,0,0,0">
                <TextBlock>视口</TextBlock>
            </Border>

            <Border
                Canvas.Left="0"
                Canvas.Top="0"
                Height="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualHeight}"
                MinWidth="70"
                VerticalAlignment="Stretch"
                Background="#f7f7f7"
                BorderBrush="#1f000000"
                BorderThickness="0,0,1,0"
                ClipToBounds="False">
                <TextBlock>音轨</TextBlock>
            </Border>
        </Canvas>

        <Grid
            Grid.Row="1"
            Grid.Column="0"
            Background="#436d9d">
            <TextBlock>控件</TextBlock>
        </Grid>
    </Grid>
</Window>
```

您已经可以稍微看到控件的结构了。
这一步并不是 alphaTab 特有的，但从结构角度来看很重要。
我们正在构建一个带有侧边栏、主视口和页脚的布局。这个结构是通过
flexbox 和绝对定位的组合组合在一起的。CSS 网格可能更适合这里，但为了更好的
浏览器支持，我们坚持使用这些技术。

内容中通过 canvas 进行绝对定位将使我们稍后能够构建一个动态扩展的侧边栏，
作为乐谱的覆盖层。

对于您的项目，您需要运用您的开发和设计技能来正确地构建元素。

:::note

从现在开始，本教程可能只会列出添加到组件的小代码片段。
每个教程部分的底部将添加应用了所有更改的最终文件。

:::

结果如下所示：

<img src="/img/tutorial-net/viewport1.png" height="400" />

## 初始化 alphaTab

现在让我们终于将 alphaTab 带入游戏中。我们在
视口中添加一个 alphaTab 控件并给它一个名称，以便我们可以在代码隐藏中访问它。
然后我们将允许用户从他的文件系统中选择一个应该打开的文件。

<Tabs
  defaultValue="xaml"
  values={[
    { label: '新 XAML', value: 'xaml', },
    { label: '新 C#', value: 'cs', },
  ]
}>
<TabItem value="xaml">

在视口上，我们添加主要的 alphaTab WPF 控件。
在控件工具栏上，我们为控件添加一些左右分隔，并在
左侧控件中添加一个打开按钮。

```xml
<!-- xmlns:wpf="clr-namespace:AlphaTab.Wpf;assembly=AlphaTab.Windows" -->
<Border
    Canvas.Left="0"
    Canvas.Top="0"
    Width="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualWidth}"
    Height="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualHeight}"
    Padding="70,0,0,0">
    <wpf:AlphaTab x:Name="AlphaTab" />
</Border>
...

<Grid
    Grid.Row="1"
    Grid.Column="0"
    Background="#436d9d">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <StackPanel
        Grid.Row="0"
        Grid.Column="0"
        Orientation="Horizontal">
        <Button Click="OnOpenClick">
            <fa:IconBlock Icon="FolderOpen" />
        </Button>
    </StackPanel>

    <StackPanel
        Grid.Row="0"
        Grid.Column="1"
        HorizontalAlignment="Right"
        Orientation="Horizontal" />
</Grid>
```

</TabItem>
<TabItem value="cs">

首先我们实现 `INotifyPropertyChanged` 以利用一些 WPF 绑定功能。
然后我们添加一些属性用于当前打开的乐谱和显示的音轨，并相应地连接它们。
在按钮点击事件中，我们显示一个文件选择器并填充加载的 Score。
我们使用 [`ScoreLoader`](/docs/guides/lowlevel-apis#loading-files-via-scoreloader) 从文件中加载乐谱。

```cs
using System;
using System.ComponentModel;
using System.IO;
using System.Runtime.CompilerServices;
using System.Windows;
using AlphaTab.Importer;
using AlphaTab.Model;
using Microsoft.Win32;

namespace AlphaTabTutorial
{
    public partial class MainWindow : INotifyPropertyChanged
    {
        private Score _score;
        private Track _selectedTrack;

        public Score Score
        {
            get => _score;
            set
            {
                if (Equals(value, _score)) return;
                _score = value;
                OnPropertyChanged();
                SelectedTrack = _score.Tracks[0];
            }
        }
        public Track SelectedTrack
        {
            get => _selectedTrack;
            set
            {
                if (Equals(value, _selectedTrack)) return;
                _selectedTrack = value;
                OnPropertyChanged();
                AlphaTab.Tracks = new[]
                {
                    value
                };
                AlphaTab.RenderTracks();
            }
        }

        public Viewport2()
        {
            InitializeComponent();
        }

        private void OnOpenClick(object sender, RoutedEventArgs e)
        {
            var dialog = new OpenFileDialog
            {
                Filter = "Supported Files (*.gp3, *.gp4, *.gp5, *.gpx, *.gp)|*.gp3;*.gp4;*.gp5;*.gpx;*.gp"
            };
            if (dialog.ShowDialog().GetValueOrDefault())
            {
                OpenFile(dialog.FileName);
            }
        }

        private void OpenFile(string fileName)
        {
            try
            {
                Score = ScoreLoader.LoadScoreFromBytes(File.ReadAllBytes(fileName));
            }
            catch (Exception e)
            {
                MessageBox.Show("Failed to open file: " + e.Message);
            }
        }

        public event PropertyChangedEventHandler PropertyChanged;
        protected virtual void OnPropertyChanged([CallerMemberName] string propertyName = null)
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
        }
    }
}
```

</TabItem>
</Tabs>

结果在选择文件后已经能够显示一些乐谱：

<img src="/img/tutorial-net/viewport2.png" height="400" />

按钮看起来还不是很好看。所以我们添加一些样式。
我们只是设置一些背景和前景来为按钮和图标着色。

```xml
<Grid
    Grid.Row="1"
    Grid.Column="0"
    Background="#436d9d">
    <Grid.Resources>
        <Style x:Key="ToolbarButtonBaseStyle" TargetType="ButtonBase">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16px" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Margin" Value="3,0" />
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Cursor" Value="{x:Static Cursors.Hand}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ButtonBase}">
                        <Border
                            Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            SnapsToDevicePixels="true">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style BasedOn="{StaticResource ToolbarButtonBaseStyle}" TargetType="Button" />
        <Style BasedOn="{StaticResource ToolbarButtonBaseStyle}" TargetType="ToggleButton">
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#5588c7" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="3,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="fa:IconBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Margin" Value="3,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Grid.Resources>
```

<img src="/img/tutorial-net/viewport2-styled.png" height="400" />

如您所见，除了与 alphaTab 控件和 API 的交互之外，
没有太多 alphaTab 特有的东西需要了解。很多事情都是普通的 C#/WPF 开发
任务，您只需要根据要构建的应用程序来规划和执行。

# 加载指示器

根据乐谱的大小，文件加载和渲染可能需要一段时间。
我们想显示一个加载指示器，直到完成。这样用户将直观地看到
文件正在加载和渲染。

为此，我们首先将使用一些 alphaTab 事件动态显示/隐藏的覆盖层扩展 XAML。

<Tabs
  defaultValue="xaml"
  values={[
    { label: '新 XAML', value: 'xaml', },
    { label: '新 C#', value: 'cs', },
  ]
}>
<TabItem value="xaml">

首先我们连接到 alphaTab 的加载事件。这非常重要：只有在控件加载后，alphaTab API 才可访问。
如果我们尝试在 alphaTab 控件加载之前访问 API 对象，它将为 null。

然后我们还向主网格添加一个新元素，这将是我们的加载指示器。我们将其绑定到一个 `LoadingIndicatorVisibility` 属性
以动态显示/隐藏它。

```xml
<Window>
...
        <wpf:AlphaTab x:Name="AlphaTab" Loaded="OnAlphaTabLoaded" />
...
        <Grid
            Grid.Row="0"
            Grid.RowSpan="2"
            Visibility="{Binding LoadingIndicatorVisibility}">
            <Border Background="#80000000" />
            <Border
                Margin="0,20,0,0"
                Padding="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Background="White">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Direction="-90"
                        Opacity="0.3" />
                </Border.Effect>
                <TextBlock>
                    乐谱正在加载
                </TextBlock>
            </Border>
        </Grid>
    </Grid>
</Window>
```

</TabItem>
<TabItem value="cs">

我们添加一个新的属性 `LoadingIndicatorVisibility`，带有更改通知，我们从 XAML 中绑定到它。
在构造函数中，我们设置数据上下文并将指示器初始化为隐藏状态，以允许打开文件。
在 `OnAlphaTabLoaded` 中，我们注册到所需的事件以动态显示/隐藏指示器。

[`RenderStarted`](/docs/reference/api/renderstarted) 将告诉我们乐谱的渲染何时开始。
[`RenderFinished`](/docs/reference/api/renderfinished) 将告诉我们乐谱的渲染何时完成。


```cs
private Visibility _loadingIndicatorVisibility = Visibility.Collapsed;
public Visibility LoadingIndicatorVisibility
{
    get => _loadingIndicatorVisibility;
    set
    {
        if (value == _loadingIndicatorVisibility) return;
        _loadingIndicatorVisibility = value;
        OnPropertyChanged();
    }
}

public MainWindow()
{
    InitializeComponent();
    DataContext = this;
}

private void OnAlphaTabLoaded(object sender, RoutedEventArgs e)
{
    AlphaTab.Api.RenderStarted.On(e =>
    {
        LoadingIndicatorVisibility = Visibility.Visible;
    });

    AlphaTab.Api.RenderFinished.On(e =>
    {
        LoadingIndicatorVisibility = Visibility.Collapsed;
    });
}
```

</TabItem>
</Tabs>

## 最终文件

<Tabs
  defaultValue="xaml"
  values={[
    { label: 'MainWindow.xaml', value: 'xaml', },
    { label: 'MainWindow.xaml.cs', value: 'cs', },
  ]
}>
<TabItem value="xaml">

```xml
<Window
    x:Class="AlphaTabTutorial.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
    xmlns:local="clr-namespace:AlphaTabTutorial"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wpf="clr-namespace:AlphaTab.Wpf;assembly=AlphaTab.Windows"
    Title="MainWindow"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance local:MainWindow}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Canvas>
            <Border
                Canvas.Left="0"
                Canvas.Top="0"
                Width="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualWidth}"
                Height="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualHeight}"
                Padding="70,0,0,0">
                <wpf:AlphaTab x:Name="AlphaTab" Loaded="OnAlphaTabLoaded" />
            </Border>

            <Border
                Canvas.Left="0"
                Canvas.Top="0"
                Height="{Binding RelativeSource={RelativeSource AncestorType=Canvas}, Path=ActualHeight}"
                MinWidth="70"
                VerticalAlignment="Stretch"
                Background="#f7f7f7"
                BorderBrush="#1f000000"
                BorderThickness="0,0,1,0"
                ClipToBounds="False">
                <TextBlock>音轨</TextBlock>
            </Border>
        </Canvas>

        <Grid
            Grid.Row="1"
            Grid.Column="0"
            Background="#436d9d">
            <Grid.Resources>
                <Style x:Key="ToolbarButtonBaseStyle" TargetType="ButtonBase">
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontSize" Value="16px" />
                    <Setter Property="Padding" Value="4" />
                    <Setter Property="Margin" Value="3,0" />
                    <Setter Property="Width" Value="40" />
                    <Setter Property="Height" Value="40" />
                    <Setter Property="Cursor" Value="{x:Static Cursors.Hand}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ButtonBase}">
                                <Border
                                    Name="Chrome"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    SnapsToDevicePixels="true">
                                    <ContentPresenter
                                        Margin="{TemplateBinding Padding}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        RecognizesAccessKey="True"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style BasedOn="{StaticResource ToolbarButtonBaseStyle}" TargetType="Button" />
                <Style BasedOn="{StaticResource ToolbarButtonBaseStyle}" TargetType="ToggleButton">
                    <Style.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#5588c7" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Margin" Value="3,0" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
                <Style TargetType="fa:IconBlock">
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Margin" Value="3,0" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Row="0"
                Grid.Column="0"
                Orientation="Horizontal">
                <Button Click="OnOpenClick">
                    <fa:IconBlock Icon="FolderOpen" />
                </Button>
            </StackPanel>

            <StackPanel
                Grid.Row="0"
                Grid.Column="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal" />
        </Grid>

        <Grid
            Grid.Row="0"
            Grid.RowSpan="2"
            Visibility="{Binding LoadingIndicatorVisibility}">
            <Border Background="#80000000" />
            <Border
                Margin="0,20,0,0"
                Padding="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Background="White">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Direction="-90"
                        Opacity="0.3" />
                </Border.Effect>
                <TextBlock>
                    乐谱正在加载
                </TextBlock>
            </Border>
        </Grid>
    </Grid>
</Window>

```

</TabItem>
<TabItem value="cs">

```cs
using System;
using System.ComponentModel;
using System.IO;
using System.Runtime.CompilerServices;
using System.Windows;
using AlphaTab.Importer;
using AlphaTab.Model;
using Microsoft.Win32;

namespace AlphaTabTutorial
{
    public partial class MainWindow : INotifyPropertyChanged
    {
        private Score _score;
        private Track _selectedTrack;

        public Score Score
        {
            get => _score;
            set
            {
                if (Equals(value, _score)) return;
                _score = value;
                OnPropertyChanged();
                SelectedTrack = _score.Tracks[0];
            }
        }
        public Track SelectedTrack
        {
            get => _selectedTrack;
            set
            {
                if (Equals(value, _selectedTrack)) return;
                _selectedTrack = value;
                OnPropertyChanged();
                AlphaTab.Tracks = new[]
                {
                    value
                };
                AlphaTab.RenderTracks();
            }
        }

        private Visibility _loadingIndicatorVisibility = Visibility.Collapsed;
        public Visibility LoadingIndicatorVisibility
        {
            get => _loadingIndicatorVisibility;
            set
            {
                if (value == _loadingIndicatorVisibility) return;
                _loadingIndicatorVisibility = value;
                OnPropertyChanged();
            }
        }

        public MainWindow()
        {
            InitializeComponent();
            DataContext = this;
        }

        private void OnAlphaTabLoaded(object sender, RoutedEventArgs e)
        {
            AlphaTab.Api.RenderStarted.On(e =>
            {
                LoadingIndicatorVisibility = Visibility.Visible;
            });

            AlphaTab.Api.RenderFinished.On(e =>
            {
                LoadingIndicatorVisibility = Visibility.Collapsed;
            });
        }

        private void OnOpenClick(object sender, RoutedEventArgs e)
        {
            var dialog = new OpenFileDialog
            {
                Filter = "Supported Files (*.gp3, *.gp4, *.gp5, *.gpx, *.gp)|*.gp3;*.gp4;*.gp5;*.gpx;*.gp"
            };
            if (dialog.ShowDialog().GetValueOrDefault())
            {
                OpenFile(dialog.FileName);
            }
        }

        private void OpenFile(string fileName)
        {
            try
            {
                Score = ScoreLoader.LoadScoreFromBytes(File.ReadAllBytes(fileName));
            }
            catch (Exception e)
            {
                MessageBox.Show("Failed to open file: " + e.Message);
            }
        }

        public event PropertyChangedEventHandler PropertyChanged;
        protected virtual void OnPropertyChanged([CallerMemberName] string propertyName = null)
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
        }
    }
}

```

</TabItem>
</Tabs>
